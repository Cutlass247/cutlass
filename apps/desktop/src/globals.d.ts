/// The shipped version, injected by vite from tauri.conf.json (see
/// vite.config.ts). Used in crash reports, where knowing which build produced
/// a stack trace is most of the value.
declare const __APP_VERSION__: string;
